name: Build and Test

on:
  push:
    branches: [develop]
      #  pull_request:
      #    branches: [develop]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Build server
      working-directory: ./server
      run: ./docker-build.sh


