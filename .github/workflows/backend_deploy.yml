name: Backend Build and Deploy

on:
  push:
    branches: [develop]
      #  pull_request:
      #    branches: [develop]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Build server
      working-directory: ./server
      run: ./docker-build.sh


